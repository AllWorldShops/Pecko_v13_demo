<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_company_form_view_inherit" model="ir.ui.view">
        <field name="name">res.company.form.inherit</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='branches']" position="after">
                <page name="purchase_limit" string="Purchase">
                    <group>
                        <field name="purchase_order_line_limit"/>
                        <div class="text-muted" style="font-size: 12px;">Defines the maximum number of lines allowed in a purchase order. When this limit is exceeded, a new purchase order will be created.</div>
                    </group>
                </page>
            </xpath>

        </field>
    </record>

</odoo>
